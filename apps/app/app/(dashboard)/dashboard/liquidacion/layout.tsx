import LiquidacionShell from '@/app/components/liquidacion/LiquidacionShell'

export default function Layout({ children }: { children: React.ReactNode }) {
  return <LiquidacionShell>{children}</LiquidacionShell>
}
